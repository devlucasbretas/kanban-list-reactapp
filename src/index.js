import ReactDOM from "react-dom";

import App from "./App";

//renderização do app na div #root do index.html
const rootElement = document.getElementById("root");
ReactDOM.render(<App />, rootElement);
